# LLM Provider: mock, openrouter, gemini, openai, or anthropic
LLM_PROVIDER=mock

# API Keys (optional — mock mode works without keys)
# These are passed as environment variables to the Docker services.
# For production, consider using Docker secrets instead.
OPENROUTER_API_KEY=
GEMINI_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# OpenRouter model. openrouter/free automatically selects an available free model.
# You can also use a specific free model ID ending in :free.
OPENROUTER_MODEL=openrouter/free

# Gemini model (Google AI Studio). e.g. gemini-2.5-flash, gemini-2.5-pro
GEMINI_MODEL=gemini-2.5-flash

# Database
DATABASE_URL=postgresql+asyncpg://conspiracy:conspiracy@localhost:5432/conspiracy
REDIS_URL=redis://localhost:6379/0

# PostgreSQL credentials (used by docker-compose)
POSTGRES_USER=conspiracy
POSTGRES_PASSWORD=conspiracy
POSTGRES_DB=conspiracy
POSTGRES_PORT=5432